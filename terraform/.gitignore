.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
terraform.tfvars
crash.log

# Written by apply: the SSH private key, and any kubeconfig you fetch. Neither
# belongs in version control -- both are live credentials to the cluster.
.ssh/
kubeconfig.yaml
