apiVersion: v1
kind: Service
metadata:
  name: k8s-defect-bot
  namespace: k8s-defect-bot
  labels:
    app.kubernetes.io/name: k8s-defect-bot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: k8s-defect-bot
  ports:
    - name: http
      port: 80
      targetPort: http
