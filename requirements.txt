fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
pydantic==2.10.5
pydantic-settings==2.7.1
kubernetes==31.0.0
# Required by FastAPI to parse the login form's POST body.
python-multipart==0.0.20

# Optional -- only needed for LLM_PROVIDER=anthropic_api (an API key, billed per
# token). LLM_PROVIDER=claude_cli needs nothing here: it shells out to the Claude
# Code CLI, which works with a Claude Pro/Max subscription. See usage.md.
anthropic==0.69.0
